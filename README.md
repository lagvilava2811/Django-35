# Django-35 Store Catalog

SQLite Django project with product and category pages.

## Run locally

```powershell
.\.venv\Scripts\python.exe manage.py runserver
```

The committed `db.sqlite3` already contains sample categories and products.
